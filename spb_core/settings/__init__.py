# Settings package — import local by default when the module is used as a package.
# Individual entry-points (manage.py, wsgi.py, asgi.py) each set
# DJANGO_SETTINGS_MODULE explicitly, so this file is mainly here to make
# `spb_core.settings` a proper Python package.
